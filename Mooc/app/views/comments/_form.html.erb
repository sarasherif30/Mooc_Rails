
<%= form_for([@lecture, @lecture.comments.build]) do |f| %>
  
    <div class="field">
    <%= f.label :content %><br>
    <%= f.text_area :content %>
  </div>

  <div class="actions">
    <%= f.submit %>

<% end %>



